/*
interface Props {
    code: Flags; (REQUIRED)
    size?: string;
    gradient?: '' | 'top-down' | 'real-circular' | 'real-linear';
    hasBorder?: boolean;
    hasDropShadow?: boolean;
    hasBorderRadius?: boolean;
    className?: string;
} */ 

import { Flag } from 'react-flagpack'
import { CircleFlag } from 'react-circle-flags'

export const FlagComponent = (props: any) => {

